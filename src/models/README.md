# Models

This package contains the model definitions and evaluation code for the project.

Current subpackages:

- `baselines/` for simple non-graph baselines
- `gnn/` for heterogeneous graph models
- `evaluation/` for model construction, training loops, and comparison entrypoints

The package is split by responsibility so model definitions stay separate from training and reporting code.
